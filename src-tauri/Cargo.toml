[package]
name = "saavi"
version = "0.1.0"
description = "Saavi — the friendliest face of GPG."
license = "MIT"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
lto = true
strip = true
