{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Saavi main window: open/save dialogs (the chosen paths are scoped by the dialog itself), text + binary file read/write for backups and sealed files, WKD key fetch over HTTPS, opening the download page in the browser, and the one-click updater (download is signature-verified against the key baked into this binary; install happens only on the user's click).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-save",
    "dialog:allow-open",
    "fs:allow-write-text-file",
    "fs:allow-write-file",
    "fs:allow-read-file",
    "opener:default",
    "process:allow-restart",
    "updater:default",
    {
      "identifier": "fs:allow-mkdir",
      "allow": [{ "path": "$TEMP/saavi-update" }]
    },
    {
      "identifier": "fs:allow-write-file",
      "allow": [{ "path": "$TEMP/saavi-update/saavi-update.deb" }]
    },
    {
      "identifier": "http:default",
      "allow": [
        {
          "url": "https://**"
        }
      ]
    }
  ]
}
