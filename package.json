{
  "name": "saavi",
  "private": true,
  "version": "0.1.0",
  "description": "Saavi (சாவி) — the friendliest face of GPG.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "openpgp": "^6.3.1"
  }
}
